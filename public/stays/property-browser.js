'use strict';
(async()=>{const grid=document.querySelector('#property-grid');if(!grid)return;const count=document.querySelector('#results-count');
 const filters={q:document.querySelector('#filter-location'),bed:document.querySelector('#filter-bedrooms'),sleep:document.querySelector('#filter-sleeps'),parking:document.querySelector('#filter-parking')};
 const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
 const card=p=>'<a class="property-card" href="/properties/'+encodeURIComponent(p.slug)+'/"><div class="property-card-media">'+(p.cover_photo?'<img src="'+esc(p.cover_photo.thumb||p.cover_photo.src)+'" alt="'+esc(p.cover_photo.alt||p.name)+'" loading="lazy" width="700" height="525">':'<div class="property-card-no-photo">Photography available on request</div>')+'</div><div class="property-card-body"><div><div class="property-location">'+esc(p.postcode||'UK')+'</div><h3>'+esc(p.name)+'</h3></div><div class="property-facts">'+[p.bedrooms?esc(p.bedrooms)+' bed'+(p.bedrooms===1?'':'s'):'',p.sleeps?'Sleeps '+esc(p.sleeps):'',p.parking?esc(p.parking):''].filter(Boolean).map(x=>'<span>'+x+'</span>').join('')+'</div><span class="property-card-cta">View property →</span></div></a>';
 try{const r=await fetch('/stays/properties.json',{cache:'no-store'});const all=(await r.json()).filter(p=>p.published);
 const render=()=>{const q=filters.q.value.trim().toLowerCase(),bed=Number(filters.bed.value||0),sleep=Number(filters.sleep.value||0),parking=filters.parking.value;const list=all.filter(p=>(!q||[p.name,p.postcode].some(v=>String(v||'').toLowerCase().includes(q)))&&(!bed||Number(p.bedrooms)>=bed)&&(!sleep||Number(p.sleeps)>=sleep)&&(!parking||(parking==='yes'?p.parking_category&&p.parking_category!=='none':p.parking_category===parking)));count.textContent=list.length+' '+(list.length===1?'property':'properties')+' displayed';grid.innerHTML=list.length?list.map(card).join(''):'<div class="empty-results">No displayed properties match those filters. Send us your requirements and we can source beyond the online collection.</div>'};
 Object.values(filters).forEach(el=>el.addEventListener(el.tagName==='INPUT'?'input':'change',render));render();
 }catch{grid.innerHTML='<div class="empty-results">The property collection could not be loaded. Please send us your requirements instead.</div>'}
})();